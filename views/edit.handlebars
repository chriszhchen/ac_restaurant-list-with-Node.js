<div class="container my-5">
  <div class="row">
    <h1 class="title mx-auto mb-4"><strong>編輯餐廳資訊</strong></h1>
  </div>
  <div class="row">
    <div class="col-md-10 col-12 mx-auto">
      {{#if validationError}}
      <div class="alert alert-danger" role="alert">
        輸入資料格式有誤！
      </div>
      {{/if}}
      <form action="/restaurants/{{ restaurant._id }}?_method=PUT" method="POST">
        <div class="form-group">
          <label for="name">餐廳名稱</label>
          <input type="text" class="form-control" id="name" name="name" placeholder="Ex. 艾朋牛排餐酒館"
            value="{{ restaurant.name }}" required>
        </div>
        <div class="form-group">
          <label for="name">餐廳英文名稱</label>
          <input type="text" class="form-control" id="name_en" name="name_en" placeholder="Ex. A Point Steak & Bar"
            value="{{ restaurant.name_en }}">
          <small class="form-text text-muted">此欄可不填</small>
        </div>
        <div class="form-row">
          <div class="form-group col-sm-6">
            <label for="category">類別</label>
            <input type="text" class="form-control" id="category" name="category" placeholder="Ex. 日本料理、美式餐廳、義式餐廳、咖啡廳 …"
              value="{{ restaurant.category }}" required>
          </div>
        </div>
        <div class="form-group">
          <label for="image">餐廳圖片</label>
          <input type="url" class="form-control" id="image" name="image" placeholder="請輸入圖片網址"
            value="{{ restaurant.image }}" required>
        </div>
        <div class="form-group">
          <label for="location">餐廳地址</label>
          <input type="text" class="form-control" id="location" name="location" placeholder="Ex. 台北市信義區忠孝東路五段 139 號 2 樓"
            value="{{ restaurant.location }}" required>
        </div>
        <div class="form-group">
          <label for="phone">餐廳電話</label>
          <input type="tel" class="form-control" id="phone" name="phone" placeholder="02 2345 6789"
            value="{{ restaurant.phone }}" pattern="[0-9]{2} [0-9]{4} [0-9]{4}" required>
          <small class="form-text text-muted">格式: 02 2345 6789</small>
        </div>
        <div class="form-group">
          <label for="google_map">Google Map連結</label>
          <input type="url" class="form-control" id="google_map" name="google_map" placeholder="請輸入Google Map連結"
            value="{{ restaurant.google_map }}" required>
        </div>
        <div class="form-row">
          <div class="form-group col-sm-3">
            <label for="rating">餐廳評分數</label>
            <input type="number" class="form-control" id="rating" name="rating" min="0" max="5" step="0.1"
              value="{{ restaurant.rating }}" required>
          </div>
        </div>
        <div class="form-group">
          <label for="description">餐廳簡介</label>
          <textarea class="form-control" id="description" name="description" rows="5" cols="30" placeholder="請輸入150字內簡介"
            maxlength="160" required>{{ restaurant.description }}</textarea>
        </div>
        <button type="submit" class="btn btn-primary">儲存編輯</button>
        <a class="btn btn-success mr-1" href="/">回到首頁</a>
      </form>
    </div>
  </div>
</div>